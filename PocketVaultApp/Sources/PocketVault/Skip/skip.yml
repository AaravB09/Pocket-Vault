# Skip configuration for PocketVault module
#
# Kotlin dependencies and Gradle build options for this module can be configured here
build:
  contents:
    - block: 'dependencies'
      contents:
        # Bumped from 2.3.1 (pre-3.0 rewrite) specifically for `surfaceType`/`isOpaque`
        # on the Scene composable — see AndroidGoalShowcaseView.swift for why.
        - 'implementation("io.github.sceneview:sceneview:4.32.0")'

# this is a transpiled Skip Lite module
skip:
  mode: 'transpiled'
